# BRAINK Deployment Surface

Deployment records belong here only when the BRAINK runtime is actually progressed into a declared target environment. Packaging or GitHub publication alone is not deployment.

Each deployment must preserve environment identity, carrier/revision identity, invocation path, start/readback evidence, rollback path, and post-deployment continuation.
