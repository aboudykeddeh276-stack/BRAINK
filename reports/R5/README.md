# Report 03 R5 — IL-LLM → KEX Symbolic/Binary Closure

This directory contains the flattened executable R5 engineering layer and evidence promoted after a clean package extraction passed 64/64 tests.

The runtime path is ordinary Python/SQLite/filesystem plumbing:

`resident IL-LLM data → semantic record → KEX translation → ToT committed state → replicated coordinate projection → Layer-2 filesystem reconciliation → optional KXBD binary boundary`.

The recovered IL-LLM corpus is identified by SHA-256 `25b59aa77b58d3445f08bee2db2a7f72b60705bf87e306db4ac2354a0fe0eed3`. Its exact 5.4 MB bytes are preserved in the verified archive and persistent Library snapshot. See `SOURCE_PROVENANCE_R5.json`.

Qualification boundary: local multi-process loopback TCP, SQLite WAL/FULL, filesystem readback, and semantic-boundary tests. This is not multi-host production consensus, TLS-authenticated transport, Byzantine tolerance, or physical-power-loss certification.