# BRAINK Local Service — Library Recovery Manifest

Status: `SOURCE_RECOVERY_ACTIVE`

This branch MUST NOT treat the experimental `braink_service.py` as a canonical BRAINK service foundation. It is retained only as an isolated test scaffold until recovered source implementations supersede it.

## Authoritative implementation rule

`Library source -> literal implementation recovery -> contract identification -> dependency rehydration -> existing test execution -> missing wiring repair -> integration candidate`

Do not invent a replacement when an existing implementation-bearing artifact can be recovered.

## Primary recovered architecture evidence

The local Library identifies `braink://local/orchestrator` as a persistent relation orchestrator. Its role is to hold tool registry, relations, continuations, evidence references, KEX delta candidates, contradictions, return routes and tool-integrity expectations. It explicitly does not flatten full tool bytes, infer parent/child lineage, or use last-writer-wins authority.

Recovered tool identities:

- `tool://server-runtime`
- `tool://research-fabric`
- `tool://IL-LLM-HCI`
- `tool://library-dna-governance`

Recovered resident traversal composition:

- `skill://knowledge/code-volume-traversal`
- `skill://runtime/resident-projection`
- `skill://runtime/failure-ledger-outbox`
- `skill://runtime/host-resource-broker`
- `skill://knowledge/resident-volume-tool-traversal`

Recovered execution invariants include:

- existing stronger mechanic precedes new construction;
- UI projection is not equivalent to backing system;
- integration is by resolution reference or adapter, not uncontrolled fusion;
- hash proves byte custody, not semantic correctness;
- failure and correction remain in lineage;
- every tool invocation returns to plan and continuation.

## Source artifacts to recover literally

1. `BRAINK_CANONICAL_BILATERAL_VOLUME_OPEN_ENDED_IL_LLM_v1_1_FILES_AS_TOOLS.json`
2. `KEDDEH_BRAINK_VOLUME_OF_VOLUMES_ORCHESTRATOR_R2.html`
3. `BRAINK_IL_LLM_FILES_AS_TOOLS_CONTROL_VOLUME_v1.html`
4. `03_TOOL_VOLUME_RELATION_GRAPH_R2.json`
5. `04_R3_TOOL_VOLUME_CONFORMANCE.json`
6. `04_ALL_170_SKILLS_DNA_REGISTRY_R1.json`
7. existing `app.py` BRAINK/KEX Service Console and its backing API/runtime files

## Promotion prohibition

No experimental service code may be promoted until the literal implementation artifacts above have been inspected for embedded source/runtime mechanics, their strongest implementations have been identified, and compatibility/adaptation has been tested. Repository presence is not product maturity.
